services:
  postgres:
    image: postgres:16-alpine
    container_name: precept-postgres
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init-db.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "127.0.0.1:5432:5432"
    networks:
      - precept-net

  redis:
    image: redis:7-alpine
    container_name: precept-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 128mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - precept-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: precept-backend
    restart: unless-stopped
    env_file: .env
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.production
      - POSTGRES_HOST=postgres
      - REDIS_URL=redis://redis:6379/0
      - REDIS_SESSIONS_URL=redis://redis:6379/1
      - CELERY_BROKER_URL=redis://redis:6379/2
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - POSTFIX_HOST=postfix
      - POSTFIX_PORT=25
    command: >
      sh -c "python manage.py create_schemas &&
             python manage.py makemigrations &&
             python manage.py migrate --database=infra &&
             python manage.py migrate --database=default &&
             python manage.py backfill_whatsapp_accounts &&
             gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 2 --threads 2"
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - static_files:/app/staticfiles
      - media_files:/app/media
      - dkim_keys:/etc/opendkim/keys:ro
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import socket; s=socket.create_connection((\"localhost\",8000),2); s.close()'"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - precept-net

  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: precept-celery-worker
    restart: unless-stopped
    env_file: .env
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.production
      - POSTGRES_HOST=postgres
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/2
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - POSTFIX_HOST=postfix
      - POSTFIX_PORT=25
    command: celery -A config worker -l info --concurrency=2
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - precept-net

  celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: precept-celery-beat
    restart: unless-stopped
    env_file: .env
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.production
      - POSTGRES_HOST=postgres
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/2
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
    command: celery -A config beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - precept-net

  channels:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: precept-channels
    restart: unless-stopped
    env_file: .env
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.production
      - POSTGRES_HOST=postgres
      - REDIS_URL=redis://redis:6379/0
    command: daphne -b 0.0.0.0 -p 8001 config.asgi:application
    ports:
      - "127.0.0.1:8001:8001"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - precept-net

  postfix:
    build:
      context: ./docker/postfix
    container_name: precept-postfix
    restart: unless-stopped
    environment:
      - MAIL_DOMAIN=${MAIL_DOMAIN:-}
      - DKIM_SELECTOR=${DKIM_SELECTOR:-mail}
    volumes:
      - dkim_keys:/etc/opendkim/keys
    networks:
      - precept-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        - NEXT_PUBLIC_API_URL=https://precept.online/api
        - NEXT_PUBLIC_WS_URL=wss://precept.online/ws/crm/
    container_name: precept-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - precept-net

volumes:
  postgres_data:
  redis_data:
  static_files:
  media_files:
  dkim_keys:

networks:
  precept-net:
    driver: bridge
